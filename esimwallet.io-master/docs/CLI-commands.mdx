import { Meta } from '@storybook/blocks';

<Meta title="CLI commands" />

# Other CLI commands

```bash'
# Clear cache (ts build, .next etc)
pnpm cache:clear

# Convenient command to test everything before pushing:
pnpm check-all

# Analyze bundle size
pnpm build:analyze

```
