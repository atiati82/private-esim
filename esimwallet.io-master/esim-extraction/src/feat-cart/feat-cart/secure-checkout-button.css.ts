import { style } from '@vanilla-extract/css';

import { cssVal } from '@/styles/css-values';

export const checkoutButton = style({});

export const checkoutButtonIcon = style({
  height: 18,
  width: 18,
  marginRight: cssVal.space.s2,
});
